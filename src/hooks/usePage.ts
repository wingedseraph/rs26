export function usePage() {
  // fix: use params of next!
  // const [searchParameters] = useSearchParams()
  // const pageParameters = searchParameters.get('page') ?? '1'
  // const page = Number(pageParameters) > 0 ? pageParameters : '1'

  return '1'
  // fix return page
}
